program: toy.py
method: grid
metric:
  goal: maximize
  name: score
parameters:
  random_seed:
    values:
      - 20
      - 40
      - 60
      - 80
      - 100
command:
  - ${env}
  - python3
  - ${program}
  - ${args_no_boolean_flags}